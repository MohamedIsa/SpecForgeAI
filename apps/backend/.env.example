DATABASE_URL=postgresql://user:password@localhost:5432/specforge
JWT_SECRET=change-me-in-production
CLAMAV_HOST=127.0.0.1
CLAMAV_PORT=3310
BRD_UPLOAD_DIR=./uploads
DEEPSEEK_API_KEY=
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
# DEV-TEMP-T1: exposes interactive Swagger UI at /docs plus a small REST
# wrapper around a few tRPC procedures under /api. Strictly opt-in: only the
# exact value "true" turns it on, everywhere, including local dev. Unset or
# any other value leaves it off.
# ENABLE_SWAGGER=true
