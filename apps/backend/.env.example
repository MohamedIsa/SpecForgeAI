DATABASE_URL=postgresql://user:password@localhost:5432/specforge
JWT_SECRET=change-me-in-production
CLAMAV_HOST=127.0.0.1
CLAMAV_PORT=3310
BRD_UPLOAD_DIR=./uploads
DEEPSEEK_API_KEY=
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat
# DEV-TEMP-T1: exposes interactive Swagger UI at /docs plus a small REST
# wrapper around a few tRPC procedures under /api. Fails closed in
# production (NODE_ENV=production) unless explicitly set to "true"; on
# everywhere else by default. Set to "false" to force it off regardless
# of NODE_ENV.
# ENABLE_SWAGGER=true
